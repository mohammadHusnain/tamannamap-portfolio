# Optional: POST endpoint for the Contact page form (e.g. a Formspree
# endpoint or your own serverless function). If unset, the form validates
# and shows a success state locally without sending data anywhere.
# Never put secret/API keys here — this value is bundled into the public
# client build and is visible to anyone who views the site source.
VITE_CONTACT_FORM_ENDPOINT=
